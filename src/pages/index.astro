---
import BaseLayout from '../layouts/BaseLayout.astro';
import NewsFeed from '../components/NewsFeed.astro';
import AlerteCitoyenne from '../components/AlerteCitoyenne.astro';
import renifleur from '../data/renifleur/latest.json';
import type { RenifleurSnapshot } from '../lib/renifleur';
import candidaturesDeclarees from '../data/elections/2027-candidatures-declarees.json';
import { partyLogoForAffiliation, partyLogoSrc } from '../lib/party-logos';
import { formatXHandle, xForAffiliation, xForCandidate } from '../lib/x-officiels';
import DualAssembleeCompare from '../components/DualAssembleeCompare.astro';
import DailyVisuals from '../components/DailyVisuals.astro';
import ScoopBand from '../components/ScoopBand.astro';
import { buildScrutinDual } from '../lib/scrutin-dual';
import {
  applyHeroDailyOverride,
  pickDailyVisuals,
  type HeroDailyOverride,
  type VisualDailyPool,
} from '../lib/visual-daily';
import { DOE_FR } from '../lib/doe';
import visualDailyPool from '../../public/illustrations/2027/visual-daily-pool.json';
import heroDailyOverrideJson from '../../public/illustrations/2027/hero-daily-override.json';
import heroKickerEditorialJson from '../../public/illustrations/2027/hero-kicker-editorial.json';

const feed = renifleur as RenifleurSnapshot;
const dailyVisual = pickDailyVisuals(visualDailyPool as VisualDailyPool);
const dailySet = applyHeroDailyOverride(
  dailyVisual.set,
  heroDailyOverrideJson as HeroDailyOverride,
  dailyVisual.dayKey,
);
const dailyBadgeLabel =
  dailySet.gallery?.[0]?.title?.trim() ||
  dailySet.heroAlt?.split('—')[0]?.trim() ||
  '1er tour';

type HeroKickerEntry = { date: string; text: string; href: string; cta?: string };
const kickerFile = heroKickerEditorialJson as {
  entries?: HeroKickerEntry[];
  fallback?: HeroKickerEntry | null;
};
const todayParis = new Intl.DateTimeFormat('en-CA', {
  timeZone: 'Europe/Paris',
  year: 'numeric',
  month: '2-digit',
  day: '2-digit',
}).format(new Date());
const heroKicker =
  kickerFile.entries?.find((e) => e.date === todayParis) ??
  kickerFile.entries?.[kickerFile.entries.length - 1] ??
  kickerFile.fallback ??
  null;

/** Fil directeur — exemple phare (présidentielle 2022 T1 vs T2) */
const dualHome = buildScrutinDual('2022-presidentielle');

type CandidatureEntry = (typeof candidaturesDeclarees.entries)[number];

const statusDeclareLabels: Record<string, string> = {
  candidature_declaree: 'Candidature déclarée',
  designe_parti: 'Désigné par le parti',
  conditionnelle: 'Sous condition',
  primaire: 'Primaire (pas ticket final)',
  retrait: 'Retrait documenté',
  substitution_parti: 'Substitution de parti (non ticket principal)',
};

const categoryOrder = ['declarees', 'parti_designe', 'conditionnelle', 'retrait'] as const;

const declaredUpdated = new Date(candidaturesDeclarees.updated).toLocaleDateString('fr-FR', {
  dateStyle: 'long',
});

const declaredByCategory = categoryOrder
  .map((id) => {
    const meta = candidaturesDeclarees.categories.find((c) => c.id === id);
    const entries = candidaturesDeclarees.entries
      .filter((e: CandidatureEntry) => e.category === id)
      .sort((a: CandidatureEntry, b: CandidatureEntry) => a.name.localeCompare(b.name, 'fr'));
    return { id, meta, entries };
  })
  .filter((g) => g.entries.length > 0);

const countDeclarees = candidaturesDeclarees.entries.filter(
  (e) => e.category === 'declarees' || e.category === 'parti_designe',
).length;
---

<BaseLayout
  title="Accueil"
  description="Le 1er tour est la photo réelle de la France politique. Atlas en données ouvertes, Assemblée AN1T, dossier 2027 (18 avril) — la démocratie avant l’élimination : faits sourcés, zéro sondage « favori », zéro classement éliminatoire."
  keywords="premier tour, présidentielle 2027, LMDPT, données ouvertes électorales, pluralité, démocratie avant élimination, atlas électoral, alerte citoyenne, programmes candidats"
>
  <DailyVisuals initialDayKey={dailyVisual.dayKey} initialIndex={dailyVisual.index} />
  <!--
    HERO INK CLAQUE (2026-07-29) — full-bleed N&B, pas le vieux mème Votez ce que vous pensez.
    Brand hero-level · 1 H1 · 1 lead · 1 CTA · plan visuel bord à bord.
  -->
  <section
    class="hero hero--ink"
    id="top"
    aria-labelledby="hero-title"
    data-daily-visual-set={dailySet.id}
  >
    <div class="hero-stage">
      <div class="hero-visual hero-visual--ink hero-visual--animated" aria-hidden="false">
        <video
          id="daily-hero-video"
          class="hero-img hero-video"
          autoplay
          muted
          loop
          playsinline
          preload="metadata"
          poster={dailySet.heroPosterWebp}
          width="1920"
          height="1080"
          aria-label={dailySet.heroAlt}
        >
          <source src={dailySet.heroVideo} type="video/mp4" />
        </video>
        <picture class="hero-static-fallback">
          <source
            id="daily-hero-webp"
            type="image/webp"
            srcset={dailySet.heroPosterWebp}
          />
          <img
            id="daily-hero-img"
            src={dailySet.heroPosterJpg}
            alt={dailySet.heroAlt}
            class="hero-img"
            width="1920"
            height="1080"
            fetchpriority="high"
            decoding="async"
          />
        </picture>
      </div>

      <div class="hero-copy">
        <p class="hero-brand">Le Média du Premier Tour</p>
        <p class="hero-kicker">
          <span class="badge-dot" aria-hidden="true"></span>
          Média civique · données ouvertes · <strong>{DOE_FR}</strong>
          <span class="sep" aria-hidden="true">·</span>
          <span id="daily-visual-badge" class="daily-visual-badge">Encre · {dailyBadgeLabel}</span>
          {
            heroKicker && (
              <>
                <span class="sep" aria-hidden="true">
                  ·
                </span>
                <a class="hero-kicker-editorial" href={heroKicker.href}>
                  {heroKicker.text}
                  {heroKicker.cta ? ` — ${heroKicker.cta}` : ''}
                </a>
              </>
            )
          }
        </p>

        <h1 class="hero-title" id="hero-title">
          La France telle qu’elle vote — pas telle qu’on l’élimine
        </h1>

        <p class="hero-lead">
          Simulez l’Assemblée du 1er tour. <strong>Faits officiels</strong> — zéro sondage
          «&nbsp;favori&nbsp;», zéro classement éliminatoire.
        </p>

        <div class="cta-row cta-row--primary" role="group" aria-label="Action principale">
          <a class="btn btn-ink" href="/analyses/assemblee-premier-tour"
            >Voir l’Assemblée du 1er tour →</a
          >
        </div>

        <p class="hero-meta meta">
          1er tour 2027&nbsp;: <strong>18 avril</strong> · 2e tour&nbsp;: <strong>2 mai</strong> ·
          data.gouv / Ministère de l’Intérieur
        </p>
      </div>
    </div>
  </section>

  <!-- P47-4 / designer 2026-07-31 — bande « À la une » sous hero (pas dans le hero) -->
  <ScoopBand />

  <!-- 3 piliers — hors first fold (P47-2 / brief Directrice 2026-07-28) -->
  <section class="trois-piliers" id="trois-piliers" aria-labelledby="trois-piliers-title">
    <h2 id="trois-piliers-title" class="visually-hidden">Trois piliers du média</h2>
    <div
      class="hero-cards pilier-cards"
      role="list"
      aria-label="Trois piliers du média"
    >
      <a href="/sources" class="hero-card pilier-card" role="listitem" data-pilier="faits">
        <div class="card-icon" aria-hidden="true">1</div>
        <div>
          <div class="pilier-kicker">Pilier 1</div>
          <div class="card-title">Faits, pas rumeurs</div>
          <div class="card-desc">
            data.gouv / Ministère. Sources primaires, licences affichées.
          </div>
        </div>
      </a>
      <a href="/atlas" class="hero-card pilier-card" role="listitem" data-pilier="pluralite">
        <div class="card-icon" aria-hidden="true">2</div>
        <div>
          <div class="pilier-kicker">Pilier 2</div>
          <div class="card-title">Toute la pluralité</div>
          <div class="card-desc">
            Atlas + AN1T : toutes les voix visibles — pas le duel.
          </div>
        </div>
      </a>
      <a href="/charte" class="hero-card pilier-card" role="listitem" data-pilier="doe">
        <div class="card-icon" aria-hidden="true">3</div>
        <div>
          <div class="pilier-kicker">Pilier 3</div>
          <div class="card-title">Vote pour, pas contre</div>
          <div class="card-desc">
            {DOE_FR}. Zéro favori. Zéro classement éliminatoire.
          </div>
        </div>
      </a>
    </div>
    <p class="hero-disclaimer meta" role="note">
      Contenu pédagogique. Les simulations (AN1T, projections 2027) ne sont
      <strong>ni des prédictions ni des consignes de vote</strong>.
    </p>
    <nav class="home-secondary-links" aria-label="Chemins secondaires">
      <a href="/analyses/presidentielle-2027-preparation">Dossier 2027 · 18 avril</a>
      <span aria-hidden="true">·</span>
      <a href="/atlas">Atlas des scrutins</a>
      <span aria-hidden="true">·</span>
      <a href="/assemblee-influenceurs">Assemblée des influenceurs</a>
      <span aria-hidden="true">·</span>
      <a href="/soutenir">Soutenir le média</a>
    </nav>
  </section>

  <!-- Manifeste court — renforce le 5 s test -->
  <section class="why-media" id="pourquoi-ce-media" aria-labelledby="why-media-title">
    <h2 id="why-media-title">Pourquoi ce média&nbsp;?</h2>
    <p class="why-media-lead">
      Le 1er tour porte des convictions. Le second tour «&nbsp;contre&nbsp;» en efface une part.
      Ici : les faits du 1er tour, pour choisir les yeux ouverts.
    </p>
    <p class="why-media-tagline">
      Faits officiels · Sans classement éliminatoire · Vote <strong>pour</strong>, pas contre.
    </p>
    <nav class="pilier-shortcuts" aria-label="Raccourcis éditoriaux">
      <a href="/assemblee-influenceurs">Qui pèse vraiment dans le débat&nbsp;?</a>
      <span aria-hidden="true">·</span>
      <a href="/analyses/presidentielle-2027-preparation">Dossier 2027</a>
      <span aria-hidden="true">·</span>
      <a href="/liberte-d-expression">Liberté d’expression</a>
      <span aria-hidden="true">·</span>
      <a href="/observatoire-censure">Observatoire de la censure</a>
      <span aria-hidden="true">·</span>
      <a href="/atlas/2022-presidentielle">Atlas 2022</a>
      <span aria-hidden="true">·</span>
      <a href="/sources">Sources</a>
    </nav>
  </section>

  <!-- Fil directeur + dual hémicycle (profondeur, après value prop) -->
  <section class="card fil-directeur" id="fil-directeur" aria-labelledby="fil-directeur-title">
    <p class="eyebrow">Fil directeur du site</p>
    <h2 id="fil-directeur-title">1er tour présidentiel → législatives — le différentiel visible</h2>
    <p>
      Sur <strong>chaque présidentielle</strong>&nbsp;: à gauche, l’Assemblée calée sur le
      <strong>1er tour présidentiel</strong> (Sainte-Laguë, proximité de programmes)&nbsp;; à droite,
      l’Assemblée réelle des <strong>législatives qui ont suivi</strong>. Tableau Δ sièges.
    </p>
    <p class="meta">
      <a href="/analyses/assemblee-premier-tour">Ouvrir le simulateur AN1T →</a>
      {' · '}
      <a href="/atlas">Tous les scrutins</a>
      {' · '}
      <a href="/atlas/2022-presidentielle">2022</a>
      {' · '}
      <a href="/atlas/2024-legislatives">Législatives 2024</a>
      {' · '}
      <a href="/analyses/gouvernance-an1t-droit">Droit &amp; gouvernance</a>
    </p>
    {
      dualHome && (
        <DualAssembleeCompare
          compact
          fullHref="/analyses/assemblee-premier-tour"
          firstRoundAlloc={dualHome.firstRound}
          realAlloc={dualHome.real}
          totalSeats={dualHome.totalSeats}
          t1Label={dualHome.t1Label}
          t2Label={dualHome.realLabel}
          firstRoundTitle={dualHome.firstRoundTitle}
          realTitle={dualHome.realTitle}
          firstRoundMeta={dualHome.firstRoundMeta}
          realMeta={dualHome.realMeta}
          realPending={dualHome.realPending}
          differential={dualHome.differential}
        />
      )
    }

  </section>

  <!-- Rubrique prioritaire : liberté d’expression (pluralité + censure) -->
  <section class="card liberte-home-card" id="liberte-expression" aria-labelledby="liberte-home-title">
    <p class="eyebrow">Rubrique prioritaire</p>
    <h2 id="liberte-home-title">
      <a href="/liberte-d-expression">Liberté d’expression</a>
    </h2>
    <p>
      Deux piliers pour un débat public digne du premier tour :
      <strong>1.</strong> pluralité de l’Assemblée ·
      <strong>2.</strong> liberté d’expression et observatoire de la censure.
    </p>
    <ul class="liberte-home-links">
      <li>
        <a href="/analyses/assemblee-premier-tour">Pluralité — simuler l’AN1T →</a>
      </li>
      <li>
        <a href="/liberte-d-expression#dossier-alerte">Liberté d’expression — alerte citoyenne →</a>
      </li>
      <li>
        <a href="/observatoire-censure/">Observatoire de la censure (depuis 2017) →</a>
      </li>
    </ul>
    <p class="meta">
      <a href="/liberte-d-expression">Ouvrir le hub Liberté d’expression →</a>
    </p>
  </section>

  <!-- Alerte citoyenne (conditions du débat public) -->
  <AlerteCitoyenne previewLimit={4} />

  {
    feed.enabled && feed.items.length > 0 && (
      <NewsFeed
        items={feed.items}
        limit={6}
        disclaimer={feed.disclaimer}
        fetchedAt={feed.fetched_at}
        feedsOk={feed.feeds_ok}
      />
    )
  }

  <section class="card highlight-card">
    <p class="eyebrow">Focus 2027 — Principal</p>
    <h3>
      <a href="/analyses/presidentielle-2027-preparation">Présidentielle 2027 : calendrier officiel & veille</a>
    </h3>
    <p>
      1er tour le <strong>18 avril 2027</strong>. Calendrier validé, candidatures en cours, programmes partiels intégrés (Attal, Bardella, Le Pen, Mélenchon, PS...).
    </p>
    <p class="meta">
      <a href="/analyses/presidentielle-2027-preparation">Lire le dossier complet 2027 →</a> · 
      <a href="/analyses/programmes/presidentielle-2027">Comparer les programmes →</a>
    </p>
  </section>

  <!-- Liste des candidatures déclarées (pas liste Conseil constitutionnel) -->
  <section class="card declared-board" id="candidatures-declarees-2027" aria-labelledby="declared-2027-title">
    <p class="eyebrow">Présidentielle 2027 — pluralité documentée</p>
    <h3 id="declared-2027-title">{candidaturesDeclarees.title}</h3>
    <p class="declared-disclaimer">{candidaturesDeclarees.disclaimer}</p>
    <p class="meta">
      <strong>{countDeclarees}</strong> candidatures déclarées / désignées en lice
      · {candidaturesDeclarees.entries.length} entrées au total (y compris conditionnelles &amp; retraits)
      · Mise à jour <time datetime={candidaturesDeclarees.updated}>{declaredUpdated}</time>
    </p>
    <p class="meta declared-legal">{candidaturesDeclarees.legal_note}</p>

    {
      declaredByCategory.map((group) => (
        <div class="declared-group">
          <h4 class="declared-group-title">
            {group.meta?.label ?? group.id}
            <span class="declared-count">{group.entries.length}</span>
          </h4>
          {group.meta?.description && <p class="meta declared-group-desc">{group.meta.description}</p>}
          <div class="declared-table-wrap" role="region" aria-label={group.meta?.label ?? group.id} tabindex="0">
            <table class="declared-table">
              <thead>
                <tr>
                  <th scope="col">Nom</th>
                  <th scope="col">Affiliation</th>
                  <th scope="col">X (officiel)</th>
                  <th scope="col">Statut</th>
                  <th scope="col">Depuis</th>
                  <th scope="col">Source</th>
                </tr>
              </thead>
              <tbody>
                {group.entries.map((c: CandidatureEntry) => {
                  const party = partyLogoForAffiliation(c.affiliation);
                  const xCand = xForCandidate(c.slug);
                  const xParty = xForAffiliation(c.affiliation);
                  return (
                    <tr class:list={[{ 'declared-row--retrait': c.category === 'retrait' }]}>
                      <td data-label="Nom">
                        <strong>{c.name}</strong>
                        {c.note && <p class="poll-note">{c.note}</p>}
                      </td>
                      <td class="poll-aff" data-label="Affiliation">
                        <span class="party-logo-row">
                          <img
                            class="party-logo"
                            src={partyLogoSrc(party)}
                            alt=""
                            width="28"
                            height="28"
                            loading="lazy"
                            decoding="async"
                          />
                          <span class="party-logo-label">{c.affiliation}</span>
                        </span>
                      </td>
                      <td class="x-links" data-label="X">
                        {xCand?.url ? (
                          <a
                            class="x-link x-link--person"
                            href={xCand.url}
                            rel="noopener noreferrer"
                            title={`X — ${c.name}`}
                          >
                            {formatXHandle(xCand.handle)}
                          </a>
                        ) : (
                          <span class="meta">—</span>
                        )}
                        {xParty?.url && (
                          <>
                            {' '}
                            <a
                              class="x-link x-link--party"
                              href={xParty.url}
                              rel="noopener noreferrer"
                              title={`X — ${xParty.label ?? c.affiliation}`}
                            >
                              parti
                            </a>
                          </>
                        )}
                      </td>
                      <td data-label="Statut">
                        <span class="poll-tag">{statusDeclareLabels[c.status] ?? c.status}</span>
                      </td>
                      <td class="declared-date" data-label="Depuis">
                        <time datetime={c.declared_at}>{c.declared_at}</time>
                      </td>
                      <td class="poll-src" data-label="Source">
                        <a href={c.source_url} rel="noopener noreferrer">
                          {c.source_label}
                        </a>
                      </td>
                    </tr>
                  );
                })}
              </tbody>
            </table>
          </div>
        </div>
      ))
    }

    <p class="meta" style="margin-top:1rem;">
      Sources :{' '}
      {candidaturesDeclarees.sources.map((s, i) => (
        <>
          {i > 0 && ' · '}
          <a href={s.url} rel="noopener noreferrer">
            {s.label}
          </a>
        </>
      ))}
      {' · '}
      <a href="/analyses/presidentielle-2027-preparation">Dossier 2027</a>
      {' · '}
      <a href="/analyses/programmes/presidentielle-2027">Programmes</a>
    </p>
  </section>

  <!-- Parcours utilisateur clair pour préparation 2027 -->
  <section class="card" aria-labelledby="parcours-2027">
    <h3 id="parcours-2027">Parcours recommandé pour se préparer au 1er tour</h3>
    <ol style="margin:0.5rem 0 0; padding-left:1.3rem; font-size:0.95rem; line-height:1.6;">
      <li>
        <a href="/analyses/assemblee-premier-tour">Simulez l’Assemblée du 1er tour</a>
        — point d’entrée principal (ajustez % et comparez scénarios)
      </li>
      <li>
        <a href="/analyses/presidentielle-2027-preparation#calendrier">Consultez le calendrier officiel 2027</a>
      </li>
      <li>
        <a href="/analyses/programmes/axes?scrutin=presidentielle-2027"
          >Programmes par grands axes</a
        >
        {' · '}
        <a href="/analyses/programmes-comparateur">comparateur</a>
      </li>
      <li>
        <a href="/assemblee-influenceurs">Assemblée des influenceurs</a>
        — qui pèse vraiment dans le débat&nbsp;?
      </li>
      <li><a href="/analyses/livres-candidats">Bibliographie des livres candidats</a></li>
      <li><a href="/atlas/2024-legislatives">Atlas et les 577 circonscriptions</a></li>
      <li><a href="/debats/assemblee-premier-tour">Débat : une Assemblée sur le 1er tour&nbsp;?</a></li>
    </ol>
    <p class="meta" style="margin-top:0.5rem;">Parcours accessible au clavier et mobile. Tout est pédagogique et sourcé.</p>
  </section>

  <section class="card">
    <p class="eyebrow">Outil clé 2027</p>
    <h3>
      <a href="/analyses/assemblee-premier-tour">Assemblée du Premier Tour 2027 — simulation interactive</a>
    </h3>
    <p>
      Hémicycle AN1T, projections de sièges à partir du 1er tour, scénarios et programmes candidats.
    </p>
    <p class="meta">
      <a href="/analyses/assemblee-premier-tour">Explorer la simulation 2027 →</a>
    </p>
  </section>

  <section class="content-section">
    <h3 class="section-title">Débats ouverts</h3>
    <ul class="card-grid">
      <li>
        <article class="card">
          <h3>
            <a href="/debats/assemblee-premier-tour">Faut-il une chambre élue sur le seul premier tour ?</a>
            <span class="status-badge status-badge--open">nouveau</span>
          </h3>
          <p>
            Simulation 2024 : hémicycle virtuel AN1T vs Assemblée réelle — carte et comparatif chiffré.
          </p>
        </article>
      </li>
      <li>
        <article class="card">
          <h3>
            <a href="/debats/vote-utile-pluralite">Le vote utile respecte-t-il la pluralité démocratique ?</a>
          </h3>
          <p>
            Positions sourcées des deux côtés — discussion modérée sur GitHub. Pas de classement
            éliminatoire.
          </p>
          <p class="meta">
            <a href="/debats">Voir tous les débats →</a>
          </p>
        </article>
      </li>
    </ul>
  </section>

  <section class="content-section">
    <h3 class="section-title">Atlas & analyses</h3>
    <ul class="card-grid">
      <li>
        <article class="card">
          <h3>
            <a href="/analyses/assemblee-premier-tour">Assemblée du premier tour — simulation 2024</a>
            <span class="status-badge status-badge--open">nouveau</span>
          </h3>
          <p>Hémicycle virtuel AN1T (577 sièges) vs Assemblée élue — comparatif chiffré.</p>
        </article>
      </li>
      <li>
        <article class="card">
          <h3><a href="/atlas/2024-legislatives">Législatives 2024 — 1er tour</a></h3>
          <p>RN 29,26 % · UG 27,99 % · ENS 20,04 % — 577 circonscriptions, data.gouv.fr.</p>
        </article>
      </li>
      <li>
        <article class="card">
          <h3><a href="/analyses/presidentielle-distorsion">Distorsion 1er / 2nd tour</a></h3>
          <p>Pluralité au premier tour, duel binaire au second — 2017 et 2022.</p>
        </article>
      </li>
      <li>
        <article class="card">
          <h3><a href="/analyses/legislatives-2024-desistements">Législatives 2024 — désistements</a></h3>
          <p>306 triangulaires potentielles → 89 après 215 désistements officiels.</p>
        </article>
      </li>
      <li>
        <article class="card">
          <h3><a href="/atlas/2022-presidentielle">Présidentielle 2022 — 1er tour</a></h3>
          <p>Macron 27,85 % · Le Pen 23,15 % · Mélenchon 21,95 %.</p>
        </article>
      </li>
      <li>
        <article class="card">
          <h3><a href="/atlas/2017-presidentielle">Présidentielle 2017 — 1er tour</a></h3>
          <p>Macron 24,01 % · Le Pen 21,30 % · quadruple polarisation.</p>
        </article>
      </li>
    </ul>
  </section>

  <!-- 
    PRINCIPE DE BASE (intégralité du site, sauf live YouTube) :
    - On expose des idées, pas des idéologies.
    - Aucun signe religieux ostentatoire, et encore moins toute autorité religieuse.
    - ne doit être diffusée.
    - Blocage immédiat de tout contenu contrevenant.
  -->
  <section class="card principles">
    <h3>Notre ligne</h3>
    <ul>
      <li><strong>Zéro biais · zéro parti pris</strong> — le média ne soutient aucun camp.</li>
      <li><strong>Transparence des couleurs politiques</strong> — posts et intervenants affichés avec teinte d’idées 1er tour (pas une carte d’adhésion).</li>
      <li><strong>Modératrice IA</strong> — aucune autorité religieuse ou idéologique ne passe les fourches caudines ; les idées politiques du débat restent bienvenues.</li>
      <li>Sources officielles traçables (<a href="/sources">data.gouv.fr</a>)</li>
      <li>Comparaisons factuelles, sans classement éliminatoire</li>
      <li><strong>{DOE_FR}</strong> — pluralité du 1er tour, sans classement ni favori.</li>
      <li>Lisibilité maximale (WCAG 2.2 AA+), revue humaine après filtre IA.</li>
      <li>Document complet : <a href="/charte"><strong>Charte publique</strong></a>.</li>
    </ul>
  </section>

  <!-- Visuels 2027 — rotation quotidienne (pool curated · Europe/Paris) -->
  <section class="visuals-section" id="visuels-2027" aria-labelledby="visuels-2027-title">
    <h3 class="section-title" id="visuels-2027-title">Croquis du jour &amp; animations — Élection 2027</h3>
    <p class="lead" style="max-width: 52rem;">
      Chaque jour, un autre regard sur le 1er tour : pluralité, mécanismes, données ouvertes.{' '}
      <strong>Croquis encre + aquarelle</strong> façon fil X — légers sur les <em>mécanismes</em>, jamais sur les personnes.
      Zéro signe religieux, zéro logo de parti, zéro caricature de camp.
    </p>

    <div class="visuals-grid">
      {
        dailySet.gallery.map((item, i) => (
          <figure class="visual-card" style={`--i: ${i}`} data-daily-gallery-item={i}>
            <img src={item.src} alt={item.alt} loading="lazy" decoding="async" />
            <figcaption>
              <span class="visual-label">{item.label}</span>
              <strong>{item.title}</strong>
              <span class="visual-wit">{item.wit}</span>
            </figcaption>
          </figure>
        ))
      }
    </div>

    <div class="video-embed">
      <h4>Animation — croquis 1er tour (6&nbsp;s)</h4>
      <video
        id="daily-gallery-video"
        class="gallery-video"
        controls
        autoplay
        muted
        loop
        playsinline
        preload="metadata"
        width="100%"
        poster={dailySet.galleryVideo?.poster ?? dailySet.heroPosterWebp}
      >
        <source src={dailySet.galleryVideo?.src ?? dailySet.heroVideo} type="video/mp4" />
        Votre navigateur ne supporte pas la vidéo.
      </video>
      <p class="meta" id="daily-gallery-caption">
        {dailySet.galleryVideo?.caption ??
          'Six secondes : croquis animé — on vote pour, pas pour éliminer.'}
      </p>
    </div>
  </section>
</BaseLayout>

<style>
  /* =====================================================
     POURQUOI CE MÉDIA — en tête de page
  ===================================================== */
  .why-media {
    margin: 0 0 1.75rem;
    padding: 1.25rem 1.35rem 1.35rem;
    background: var(--card);
    border: 1px solid var(--border);
    border-radius: var(--radius-lg, 12px);
    border-left: 4px solid var(--accent, #2563a8);
    max-width: 52rem;
  }
  .why-media h2 {
    margin: 0 0 0.65rem;
    font-size: 1.45rem;
    line-height: 1.25;
    color: #0a0a0a !important;
  }
  .why-media-lead {
    margin: 0 0 0.75rem;
    font-size: 1.05rem;
    line-height: 1.55;
    color: #0a0a0a !important;
    max-width: 46rem;
  }
  .why-media-tagline {
    margin: 0;
    font-size: 0.98rem;
    line-height: 1.45;
    color: #0a0a0a !important;
    font-weight: 600;
  }
  .why-media-tagline strong {
    color: #0a0a0a !important;
  }
  @media (prefers-color-scheme: dark) {
    .why-media h2,
    .why-media-lead,
    .why-media-tagline,
    .why-media-tagline strong {
      color: #f5f2eb !important;
    }
  }

  /* =====================================================
     HERO REDESIGN — Professional, high-contrast, inclusive
     - WCAG 2.2 AA focus (contrast + visible focus states)
     - Mobile-first
     - Neutral civic aesthetic
     - Strong dark mode support (per design brief)
  ===================================================== */

  /* —— Hero encre N&B full-bleed (2026-07-29) —— */
  .hero--ink {
    --ink-bg: #050505;
    --ink-fg: #fafafa;
    --ink-muted: #c8c8c8;
    width: 100vw;
    max-width: 100vw;
    margin: 0 0 2rem;
    margin-left: calc(50% - 50vw);
    margin-right: calc(50% - 50vw);
    padding: 0;
    background: var(--ink-bg);
    border-bottom: 0;
    color: var(--ink-fg);
    overflow: clip;
  }

  .hero-stage {
    display: grid;
    grid-template-columns: 1fr;
    gap: 0;
  }

  @media (min-width: 960px) {
    .hero-stage {
      grid-template-columns: 1.15fr 0.85fr;
      min-height: min(72vh, 40rem);
      align-items: stretch;
    }
  }

  .hero-visual--ink {
    margin: 0;
    min-height: clamp(14rem, 42vw, 28rem);
    background: #000;
  }

  @media (min-width: 960px) {
    .hero-visual--ink {
      min-height: 100%;
    }
  }

  .hero--ink .hero-img {
    width: 100%;
    height: 100%;
    max-height: none;
    min-height: clamp(14rem, 42vw, 28rem);
    object-fit: cover;
    object-position: left center;
    border-radius: 0;
    box-shadow: none;
    border: 0;
    background: #000;
    filter: grayscale(1) contrast(1.18);
  }

  @media (min-width: 960px) {
    .hero--ink .hero-img {
      position: absolute;
      inset: 0;
      min-height: 100%;
    }
    .hero-visual--ink {
      position: relative;
    }
  }

  .hero--ink .hero-video {
    animation: lmdpt-ink-breathe 7s ease-in-out infinite;
  }

  @keyframes lmdpt-ink-breathe {
    0%, 100% { transform: scale(1); }
    50% { transform: scale(1.018); }
  }

  .hero-copy {
    display: flex;
    flex-direction: column;
    justify-content: center;
    gap: 0;
    padding: clamp(1.35rem, 3.5vw, 2.75rem) clamp(1.1rem, 4vw, 2.75rem);
    background: var(--ink-bg);
    max-width: none;
  }

  .hero-brand {
    margin: 0 0 0.55rem;
    font-family: 'Palatino Linotype', Palatino, 'Book Antiqua', Georgia, serif;
    font-size: clamp(1.35rem, 2.8vw, 1.85rem);
    font-weight: 700;
    letter-spacing: -0.02em;
    line-height: 1.15;
    color: var(--ink-fg) !important;
    animation: lmdpt-rise 0.5s ease both;
  }

  .hero-kicker {
    display: flex;
    align-items: center;
    flex-wrap: wrap;
    gap: 0.35rem 0.5rem;
    margin: 0 0 1rem;
    font-family: 'Iowan Old Style', 'Palatino Linotype', Palatino, Georgia, serif;
    font-size: 0.88rem;
    letter-spacing: 0.04em;
    text-transform: uppercase;
    color: var(--ink-muted) !important;
    line-height: 1.4;
    animation: lmdpt-rise 0.5s ease 0.04s both;
  }

  .hero-kicker .sep { opacity: 0.45; }
  .hero-kicker strong { color: var(--ink-fg); font-weight: 700; }

  .hero-kicker-editorial {
    color: var(--ink-fg) !important;
    text-decoration: underline;
    text-underline-offset: 0.15em;
    text-transform: none;
    letter-spacing: 0.01em;
    font-weight: 600;
  }
  .hero-kicker-editorial:hover,
  .hero-kicker-editorial:focus-visible {
    opacity: 0.9;
    outline: none;
  }

  .badge-dot {
    display: inline-block;
    width: 7px;
    height: 7px;
    background: #fff;
    border-radius: 50%;
    animation: lmdpt-pulse 1.8s ease-in-out infinite;
  }

  .daily-visual-badge {
    font-size: 0.92em;
    font-weight: 600;
    letter-spacing: 0.06em;
    color: #fff;
  }

  .hero--ink .hero-title {
    font-family: 'Palatino Linotype', Palatino, 'Book Antiqua', Georgia, serif;
    font-size: clamp(1.85rem, 4.6vw, 3rem);
    line-height: 1.05;
    margin: 0 0 0.95rem;
    letter-spacing: -0.03em;
    font-weight: 700;
    color: var(--ink-fg) !important;
    max-width: 16ch;
    animation: lmdpt-rise 0.55s ease 0.08s both;
  }

  .hero--ink .hero-lead {
    font-size: 1.08rem;
    line-height: 1.5;
    margin: 0 0 1.25rem;
    color: var(--ink-muted) !important;
    max-width: 38ch;
    animation: lmdpt-rise 0.55s ease 0.12s both;
  }

  .hero--ink .hero-lead strong {
    color: var(--ink-fg);
  }

  .hero-meta {
    margin: 0.85rem 0 0;
    color: var(--ink-muted) !important;
    font-size: 0.92rem;
  }
  .hero-meta strong { color: var(--ink-fg); }

  .cta-row {
    display: flex;
    flex-wrap: wrap;
    gap: 0.55rem;
    margin: 0 0 0.35rem;
    animation: lmdpt-rise 0.55s ease 0.16s both;
  }
  .cta-row--primary .btn {
    flex: 0 1 auto;
    min-width: min(100%, 16rem);
  }
  .cta-row .btn {
    flex: 1 1 auto;
    min-width: min(100%, 11.5rem);
  }

  .btn-ink {
    background: #fff !important;
    color: #050505 !important;
    border: 2px solid #fff !important;
    font-weight: 700;
    letter-spacing: 0.01em;
    transition: transform 0.2s ease, background 0.2s ease, color 0.2s ease;
  }
  .btn-ink:hover {
    background: transparent !important;
    color: #fff !important;
    transform: translateY(-2px);
  }
  .btn-ink:focus-visible {
    outline: 3px solid #fff;
    outline-offset: 3px;
  }

  .btn-primary {
    background: var(--accent) !important;
    color: var(--on-accent) !important;
    border: 2px solid var(--accent) !important;
  }

  .btn-primary:hover {
    filter: brightness(1.08);
    color: var(--on-accent) !important;
  }
  .btn-secondary {
    background: var(--card) !important;
    color: var(--accent) !important;
    border: 2px solid var(--accent) !important;
  }

  @media (prefers-reduced-motion: reduce) {
    .hero--ink .hero-video,
    .badge-dot {
      animation: none !important;
    }
  }
  .highlight-card h3 {
    margin: 0.35rem 0 0.65rem;
    font-size: 1.15rem;
    color: var(--fg) !important;
  }
  .highlight-card h3 a {
    color: var(--accent) !important;
    text-decoration: none;
  }
  .highlight-card h3 a:hover {
    filter: brightness(1.1);
  }
  .highlight-card p {
    color: var(--fg) !important;
  }
  .content-section {
    margin-bottom: 2rem;
  }
  .principles ul {
    margin: 0.5rem 0 0;
    padding-left: 1.2rem;
  }

  /* 3 piliers éditoriaux (P15-1 / P47-2) — hors first fold */
  .trois-piliers {
    margin: 0 0 1.5rem;
  }
  .visually-hidden {
    position: absolute;
    width: 1px;
    height: 1px;
    padding: 0;
    margin: -1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
    border: 0;
  }
  .home-secondary-links {
    display: flex;
    flex-wrap: wrap;
    gap: 0.35rem 0.55rem;
    align-items: center;
    margin: 0.85rem 0 0;
    font-size: 0.95rem;
  }
  .home-secondary-links a {
    color: var(--accent);
    font-weight: 600;
    text-decoration: none;
  }
  .home-secondary-links a:hover {
    text-decoration: underline;
  }
  .home-secondary-links a:focus-visible {
    outline: 3px solid var(--accent);
    outline-offset: 2px;
  }
  .hero-cards {
    display: grid;
    grid-template-columns: 1fr;
    gap: 0.75rem;
    margin-top: 0;
  }

  @media (min-width: 720px) {
    .hero-cards {
      grid-template-columns: repeat(3, 1fr);
    }
  }
  @media (min-width: 520px) and (max-width: 719px) {
    .hero-cards {
      grid-template-columns: 1fr;
    }
  }

  .hero-card {
    display: flex;
    gap: 0.75rem;
    padding: 1rem 1rem;
    background: var(--card) !important;
    border: 1px solid var(--border);
    border-radius: var(--radius-lg);
    text-decoration: none;
    color: var(--fg) !important;
    transition: border-color 0.2s ease, box-shadow 0.2s ease, transform 0.2s cubic-bezier(0.34, 1.2, 0.64, 1);
    animation: lmdpt-rise 0.55s ease both;
    animation-delay: calc(0.12s * var(--card-i, 0));
    min-height: 5.5rem;
  }

  .hero-card:nth-child(1) { --card-i: 1; }
  .hero-card:nth-child(2) { --card-i: 2; }
  .hero-card:nth-child(3) { --card-i: 3; }

  .pilier-card[data-pilier='faits'] {
    border-top: 3px solid #1d4ed8;
  }
  .pilier-card[data-pilier='pluralite'] {
    border-top: 3px solid #059669;
  }
  .pilier-card[data-pilier='doe'] {
    border-top: 3px solid var(--accent, #c2410c);
  }

  .pilier-kicker {
    font-size: 0.7rem;
    font-weight: 700;
    letter-spacing: 0.04em;
    text-transform: uppercase;
    color: var(--muted) !important;
    margin-bottom: 0.15rem;
  }

  .hero-card:hover {
    border-color: var(--accent);
    box-shadow: 0 8px 20px -4px rgb(0 0 0 / 0.12);
    transform: translateY(-4px);
  }

  .hero-card:focus-visible {
    outline: 3px solid var(--accent);
    outline-offset: 2px;
  }

  .hero-card .card-icon {
    font-family: var(--font-sans, system-ui, sans-serif);
    font-size: 0.85rem;
    font-weight: 700;
    line-height: 1;
    margin-top: 2px;
    flex-shrink: 0;
    width: 1.75rem;
    height: 1.75rem;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    border-radius: 999px;
    background: var(--accent-soft, #eef1f6);
    color: var(--fg);
  }

  .pilier-card[data-pilier='faits'] .card-icon {
    background: color-mix(in srgb, #1d4ed8 14%, white);
    color: #1d4ed8;
  }
  .pilier-card[data-pilier='pluralite'] .card-icon {
    background: color-mix(in srgb, #059669 14%, white);
    color: #047857;
  }
  .pilier-card[data-pilier='doe'] .card-icon {
    background: color-mix(in srgb, var(--accent, #c2410c) 14%, white);
    color: var(--accent, #c2410c);
  }

  .hero-card .card-title {
    font-weight: 700;
    font-size: 0.97rem;
    line-height: 1.25;
    margin-bottom: 0.25rem;
    color: var(--fg) !important;
  }

  .hero-card .card-desc {
    font-size: 0.88rem;
    color: var(--muted) !important;
    line-height: 1.45;
  }

  .hero-disclaimer {
    margin: 0.85rem 0 0;
    max-width: 70ch;
    line-height: 1.45;
  }

  .pilier-shortcuts {
    margin-top: 0.85rem;
    display: flex;
    flex-wrap: wrap;
    gap: 0.35rem 0.5rem;
    font-size: 0.92rem;
  }

  .pilier-shortcuts a {
    font-weight: 600;
  }

  .pilier-shortcuts a:focus-visible {
    outline: 3px solid var(--accent);
    outline-offset: 2px;
  }

  .liberte-home-card .liberte-home-links {
    margin: 0.5rem 0 0.75rem;
    padding-left: 1.15rem;
  }

  .liberte-home-card .liberte-home-links li {
    margin: 0.35rem 0;
  }

  /* Hero visual base — ink overrides via .hero--ink .hero-img */
  .hero-visual {
    width: 100%;
    position: relative;
  }

  .hero-visual--animated {
    isolation: isolate;
  }

  .hero-static-fallback {
    display: none;
  }

  .hero--ink .hero-video {
    position: relative;
    z-index: 1;
  }

  .visuals-section {
    margin-top: 2.5rem;
  }
  .visuals-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(min(100%, 220px), 1fr));
    gap: 1rem;
    margin: 1.25rem 0 2rem;
  }
  .visual-card {
    margin: 0;
    background: var(--card);
    border: 1px solid var(--border);
    border-radius: var(--radius-lg);
    overflow: hidden;
    box-shadow: var(--shadow);
    transition: transform 0.25s cubic-bezier(0.34, 1.2, 0.64, 1), box-shadow 0.25s ease, border-color 0.2s ease;
    animation: lmdpt-rise 0.55s ease both;
    animation-delay: calc(0.07s * var(--i, 0));
  }
  .visual-card:hover {
    transform: translateY(-5px) rotate(-0.4deg);
    box-shadow: 0 12px 28px -8px rgb(0 0 0 / 0.16);
    border-color: var(--accent);
  }
  .visual-card img {
    width: 100%;
    height: auto;
    display: block;
    aspect-ratio: 1 / 1;
    object-fit: cover;
    transition: transform 0.45s ease;
  }
  .visual-card:hover img {
    transform: scale(1.04);
  }
  .gallery-video {
    max-width: 820px;
    width: 100%;
    border-radius: 8px;
    box-shadow: var(--shadow);
    border: 1px solid var(--border);
    background: var(--card);
  }

  @keyframes lmdpt-pulse {
    0%, 100% { opacity: 1; transform: scale(1); box-shadow: 0 0 0 0 color-mix(in srgb, var(--accent) 45%, transparent); }
    50% { opacity: 0.85; transform: scale(1.15); box-shadow: 0 0 0 6px transparent; }
  }
  @keyframes lmdpt-float {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-6px); }
  }
  @keyframes lmdpt-rise {
    from { opacity: 0; transform: translateY(12px); }
    to { opacity: 1; transform: translateY(0); }
  }

  @media (prefers-reduced-motion: reduce) {
    .badge-dot,
    .hero-video,
    .hero-title,
    .hero-lead,
    .cta-row,
    .hero-badge,
    .hero-card,
    .visual-card {
      animation: none !important;
    }
    .hero-card:hover,
    .visual-card:hover,
    .visual-card:hover img {
      transform: none;
    }
    .hero-video {
      display: none;
    }
    .hero-static-fallback {
      display: block;
    }
  }
  .visual-card figcaption {
    display: flex;
    flex-direction: column;
    gap: 0.2rem;
    padding: 0.65rem 0.8rem 0.75rem;
    font-size: 0.9rem;
    color: var(--fg);
    background: var(--bg);
    border-top: 1px solid var(--border);
    line-height: 1.35;
  }
  .visual-card .visual-label {
    font-size: 0.72rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.04em;
    color: var(--muted);
  }
  .visual-card figcaption strong {
    font-size: 0.95rem;
    color: var(--fg);
  }
  .visual-card .visual-wit {
    font-size: 0.86rem;
    color: var(--muted);
    font-style: italic;
    line-height: 1.4;
  }
  .video-embed {
    margin-top: 1.5rem;
    max-width: 860px;
  }
  .video-embed h4 {
    margin-bottom: 0.5rem;
    font-size: 1.05rem;
  }

  /* === Candidatures déclarées 2027 (accueil) === */
  .declared-board {
    margin-top: 0.25rem;
  }
  .declared-disclaimer,
  .declared-legal {
    font-size: 0.95rem;
    line-height: 1.5;
    color: var(--fg) !important;
    margin: 0.5rem 0 0.65rem;
    max-width: 62rem;
  }
  .declared-group {
    margin-top: 1.25rem;
  }
  .declared-group-title {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 1.05rem;
    margin: 0 0 0.35rem;
    color: var(--fg) !important;
  }
  .declared-count {
    font-size: 0.8rem;
    font-weight: 600;
    padding: 0.1rem 0.45rem;
    border-radius: 999px;
    border: 1px solid var(--border);
    background: var(--bg);
    color: var(--muted) !important;
  }
  .declared-group-desc {
    margin: 0 0 0.5rem;
  }
  .declared-table-wrap {
    overflow-x: auto;
    margin: 0.5rem 0 0;
    border: 1px solid var(--border);
    border-radius: var(--radius-lg);
    -webkit-overflow-scrolling: touch;
  }
  .declared-table {
    width: 100%;
    border-collapse: collapse;
    font-size: 0.92rem;
    min-width: 0;
  }
  .declared-table th,
  .declared-table td {
    padding: 0.65rem 0.75rem;
    text-align: left;
    vertical-align: top;
    border-bottom: 1px solid var(--border);
    color: var(--fg) !important;
  }
  .declared-table th {
    background: var(--bg);
    font-weight: 600;
    font-size: 0.85rem;
    position: sticky;
    top: 0;
    z-index: 1;
  }
  .declared-table tbody tr:last-child td {
    border-bottom: none;
  }

  /* Desktop / tablette large : largeur mini pour lisibilité colonnes */
  @media (min-width: 761px) {
    .declared-table {
      min-width: 640px;
    }
  }

  /* Mobile : tableau → cartes empilées (plus de scroll horizontal forcé) */
  @media (max-width: 760px) {
    .declared-table-wrap {
      border: none;
      overflow: visible;
      background: transparent;
    }
    .declared-table,
    .declared-table tbody,
    .declared-table tr,
    .declared-table td {
      display: block;
      width: 100%;
    }
    .declared-table thead {
      position: absolute;
      width: 1px;
      height: 1px;
      padding: 0;
      margin: -1px;
      overflow: hidden;
      clip: rect(0, 0, 0, 0);
      white-space: nowrap;
      border: 0;
    }
    .declared-table tbody {
      display: grid;
      gap: 0.75rem;
    }
    .declared-table tr {
      border: 1px solid var(--border);
      border-radius: var(--radius-lg, 12px);
      padding: 0.7rem 0.8rem 0.55rem;
      background: var(--bg);
      box-shadow: 0 1px 2px rgba(0, 0, 0, 0.04);
    }
    .declared-table td {
      display: grid;
      grid-template-columns: minmax(5.5rem, 32%) 1fr;
      gap: 0.35rem 0.65rem;
      padding: 0.35rem 0;
      border-bottom: 1px dashed color-mix(in srgb, var(--border) 70%, transparent);
      min-width: 0;
    }
    .declared-table td:last-child {
      border-bottom: none;
    }
    .declared-table td::before {
      content: attr(data-label);
      font-family: system-ui, sans-serif;
      font-size: 0.72rem;
      font-weight: 700;
      letter-spacing: 0.03em;
      text-transform: uppercase;
      color: var(--muted) !important;
      padding-top: 0.15rem;
    }
    .declared-table td[data-label='Nom'] {
      grid-template-columns: 1fr;
      gap: 0.2rem;
      padding-bottom: 0.5rem;
      margin-bottom: 0.15rem;
      border-bottom: 1px solid var(--border);
    }
    .declared-table td[data-label='Nom']::before {
      display: none;
    }
    .declared-table td[data-label='Nom'] strong {
      font-size: 1.02rem;
      line-height: 1.3;
    }
    .x-links {
      white-space: normal;
    }
    .poll-aff,
    .poll-src {
      max-width: none;
    }
    .party-logo-row {
      flex-wrap: wrap;
    }
  }
  .declared-row--retrait {
    opacity: 0.72;
  }
  .declared-date {
    font-variant-numeric: tabular-nums;
    white-space: nowrap;
    color: var(--muted) !important;
  }
  .poll-tag {
    display: inline-block;
    font-size: 0.72rem;
    font-weight: 600;
    letter-spacing: 0.02em;
    padding: 0.12rem 0.4rem;
    border-radius: 999px;
    border: 1px solid var(--border);
    background: var(--bg);
    color: var(--muted) !important;
    white-space: nowrap;
    margin-left: 0.35rem;
    vertical-align: middle;
  }
  .poll-note {
    margin: 0.25rem 0 0;
    font-size: 0.82rem;
    color: var(--muted) !important;
    line-height: 1.35;
  }
  .x-links {
    white-space: nowrap;
    font-size: 0.88rem;
  }
  .x-link {
    color: var(--accent, #2563a8) !important;
    text-decoration: none;
    font-weight: 600;
  }
  .x-link:hover,
  .x-link:focus-visible {
    text-decoration: underline;
  }
  .x-link--party {
    font-weight: 500;
    font-size: 0.8rem;
    opacity: 0.9;
  }
  .poll-aff {
    font-size: 0.88rem;
    color: var(--muted) !important;
    max-width: 16rem;
  }
  .party-logo-row {
    display: inline-flex;
    align-items: center;
    gap: 0.55rem;
    max-width: 100%;
  }
  .party-logo {
    flex: 0 0 auto;
    width: 28px;
    height: 28px;
    border-radius: 7px;
    object-fit: cover;
    border: 1px solid color-mix(in srgb, var(--border) 80%, transparent);
    background: var(--bg);
    box-shadow: 0 1px 2px rgba(0, 0, 0, 0.06);
  }
  .party-logo-label {
    line-height: 1.25;
    color: var(--fg) !important;
  }
  .poll-src {
    font-size: 0.82rem;
    line-height: 1.35;
    max-width: 16rem;
  }
  .poll-src a {
    color: var(--accent) !important;
  }

  /* Fil directeur : éviter double cadre trop large sur mobile */
  .fil-directeur {
    overflow: hidden;
  }
  .fil-directeur .dual-assemblee {
    margin-bottom: 0.35rem;
  }

  /* Accueil — responsive finitions */
  @media (max-width: 640px) {
    .hero {
      margin-bottom: 1.35rem;
      padding-bottom: 1.25rem;
    }
    .hero-title {
      font-size: clamp(1.45rem, 6.2vw, 1.85rem);
      line-height: 1.15;
      letter-spacing: -0.02em;
    }
    .hero-lead {
      font-size: 1.02rem;
      max-width: none;
    }
    .cta-row {
      flex-direction: column;
      gap: 0.5rem;
    }
    .cta-row .btn {
      width: 100%;
      min-width: 0;
      justify-content: center;
      display: inline-flex;
      align-items: center;
      min-height: 48px;
      font-size: 0.95rem;
      padding: 0.7rem 1rem;
    }
    .hero-card {
      min-height: 0;
      padding: 0.9rem 0.95rem;
    }
    .hero-card .card-desc {
      font-size: 0.86rem;
    }
    .why-media {
      padding: 1rem 1rem 1.1rem;
      margin-bottom: 1.25rem;
    }
    .why-media h2 {
      font-size: 1.25rem;
    }
    .visuals-grid {
      grid-template-columns: 1fr;
      gap: 0.85rem;
    }
    .pilier-shortcuts {
      font-size: 0.88rem;
      gap: 0.3rem 0.45rem;
    }
    .hero-img {
      max-height: 280px;
      object-fit: cover;
    }
  }

  @media (min-width: 641px) and (max-width: 859px) {
    .hero-grid {
      gap: 1.25rem;
    }
    .cta-row .btn {
      flex: 1 1 calc(50% - 0.4rem);
    }
  }

  @media (min-width: 860px) {
    .hero {
      padding-top: 0.75rem;
    }
    .cta-row .btn {
      flex: 0 1 auto;
    }
  }
</style>