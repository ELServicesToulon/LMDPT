---
import BaseLayout from '../layouts/BaseLayout.astro';
import NewsFeed from '../components/NewsFeed.astro';
import AlerteCitoyenne from '../components/AlerteCitoyenne.astro';
import renifleur from '../data/renifleur/latest.json';
import type { RenifleurSnapshot } from '../lib/renifleur';
import candidaturesDeclarees from '../data/elections/2027-candidatures-declarees.json';
import { partyLogoForAffiliation, partyLogoSrc } from '../lib/party-logos';
import { formatXHandle, xForAffiliation, xForCandidate } from '../lib/x-officiels';
import DualAssembleeCompare from '../components/DualAssembleeCompare.astro';
import { buildScrutinDual } from '../lib/scrutin-dual';

const feed = renifleur as RenifleurSnapshot;
/** Fil directeur — exemple phare (présidentielle 2022 T1 vs T2) */
const dualHome = buildScrutinDual('2022-presidentielle');

type CandidatureEntry = (typeof candidaturesDeclarees.entries)[number];

const statusDeclareLabels: Record<string, string> = {
  candidature_declaree: 'Candidature déclarée',
  designe_parti: 'Désigné par le parti',
  conditionnelle: 'Sous condition',
  primaire: 'Primaire (pas ticket final)',
  retrait: 'Retrait documenté',
  substitution_parti: 'Substitution de parti (non ticket principal)',
};

const categoryOrder = ['declarees', 'parti_designe', 'conditionnelle', 'retrait'] as const;

const declaredUpdated = new Date(candidaturesDeclarees.updated).toLocaleDateString('fr-FR', {
  dateStyle: 'long',
});

const declaredByCategory = categoryOrder
  .map((id) => {
    const meta = candidaturesDeclarees.categories.find((c) => c.id === id);
    const entries = candidaturesDeclarees.entries
      .filter((e: CandidatureEntry) => e.category === id)
      .sort((a: CandidatureEntry, b: CandidatureEntry) => a.name.localeCompare(b.name, 'fr'));
    return { id, meta, entries };
  })
  .filter((g) => g.entries.length > 0);

const countDeclarees = candidaturesDeclarees.entries.filter(
  (e) => e.category === 'declarees' || e.category === 'parti_designe',
).length;
---

<BaseLayout
  title="Accueil"
  description="Le premier tour : le miroir le plus fidèle de la France politique. Atlas open data, présidentielle 2027 (18 avril), programmes sourcés, alerte citoyenne — Democracy Over Elimination, sans sondages ni tier list."
  keywords="premier tour, présidentielle 2027, LMDPT, open data élections, pluralité, Democracy Over Elimination, atlas électoral, alerte citoyenne, programmes candidats"
>
  <!--
    HERO FIRST (Créateur de site · cycle 2026-07-17)
    Pattern adapté (pas copié) des leaders news : value prop ≤ 5 s + CTA piliers above the fold.
    Hémicycle dual déplacé sous le hero (profondeur, pas premier écran).
  -->
  <section class="hero" id="top" aria-labelledby="hero-title">
    <div class="hero-badge">
      <span class="badge-dot"></span>
      <span>Média civique · open data</span>
      <span class="sep">•</span>
      <strong>Democracy Over Elimination</strong>
    </div>

    <div class="hero-grid">
      <div class="hero-visual hero-visual--animated">
        <!-- Croquis X humoristique + micro-vidéo (poster = LCP webp/jpeg) · 2026-07-18 -->
        <video
          class="hero-img hero-video"
          autoplay
          muted
          loop
          playsinline
          preload="metadata"
          poster="/illustrations/2027/hero-premier-tour-2027.webp"
          width="1280"
          height="720"
          aria-label="Animation croquis humoristique : pluralité du premier tour, urne transparente et idées colorées"
        >
          <source src="/videos/2027/hero-voting-animation.mp4" type="video/mp4" />
        </video>
        <picture class="hero-static-fallback">
          <source
            type="image/webp"
            srcset="/illustrations/2027/hero-premier-tour-2027.webp"
          />
          <img
            src="/illustrations/2027/hero-premier-tour-2027.jpg"
            alt="Croquis humoristique style X : citoyens et urne transparente du 1er tour débordant d’idées colorées — illustration civique laïque."
            class="hero-img"
            width="1280"
            height="720"
            fetchpriority="high"
            decoding="async"
          />
        </picture>
      </div>

      <div class="hero-inner">
        <h1 class="hero-title" id="hero-title">
          Le premier tour, sans filtre&nbsp;: la photo réelle de la France politique
        </h1>

        <p class="hero-lead">
          Atlas open data, simulation d’Assemblée du 1er tour, dossier 2027.
          <strong>Faits sourcés</strong> — pas de sondages «&nbsp;favori&nbsp;», pas de tier list.
        </p>

        <div class="cta-row" role="group" aria-label="Actions principales">
          <a class="btn btn-primary" href="/analyses/assemblee-premier-tour"
            >Simuler l’Assemblée du 1er tour →</a
          >
          <a class="btn btn-secondary" href="/analyses/presidentielle-2027-preparation"
            >Dossier présidentielle 2027</a
          >
          <a class="btn btn-secondary" href="/atlas">Atlas des scrutins</a>
        </div>

        <p class="meta">
          1er tour 2027&nbsp;: <strong>18 avril</strong> · 2e tour&nbsp;: <strong>2 mai</strong> · Sources
          data.gouv / Ministère de l’Intérieur
        </p>
      </div>
    </div>

    <!-- 3 piliers éditoriaux (P15-1 / P8-7) — above the fold, WCAG focus rings -->
    <div
      class="hero-cards pilier-cards"
      role="list"
      aria-label="Trois piliers du média"
    >
      <a href="/sources" class="hero-card pilier-card" role="listitem" data-pilier="faits">
        <div class="card-icon" aria-hidden="true">🔒</div>
        <div>
          <div class="pilier-kicker">Pilier 1</div>
          <div class="card-title">Sécurité &amp; faits</div>
          <div class="card-desc">
            Open data officiels (data.gouv / Ministère). Sources primaires, licences affichées — pas de rumeur.
          </div>
        </div>
      </a>
      <a href="/atlas" class="hero-card pilier-card" role="listitem" data-pilier="pluralite">
        <div class="card-icon" aria-hidden="true">🌈</div>
        <div>
          <div class="pilier-kicker">Pilier 2</div>
          <div class="card-title">Pluralité réelle</div>
          <div class="card-desc">
            Atlas 1er tour + simulation AN1T : toutes les voix visibles, sans réduction binaire.
          </div>
        </div>
      </a>
      <a href="/charte" class="hero-card pilier-card" role="listitem" data-pilier="doe">
        <div class="card-icon" aria-hidden="true">🗳️</div>
        <div>
          <div class="pilier-kicker">Pilier 3</div>
          <div class="card-title">Democracy Over Elimination</div>
          <div class="card-desc">
            Vote <strong>pour</strong>, pas contre. Pas de sondages «&nbsp;favori&nbsp;», pas de tier list.
          </div>
        </div>
      </a>
    </div>
    <p class="hero-disclaimer meta" role="note">
      Contenu pédagogique et documentaire. Les simulations (AN1T, projections 2027) ne sont
      <strong>ni des prédictions ni des consignes de vote</strong>.
    </p>
  </section>

  <!-- Manifeste court — renforce le 5 s test -->
  <section class="why-media" id="pourquoi-ce-media" aria-labelledby="why-media-title">
    <h2 id="why-media-title">Pourquoi ce média&nbsp;?</h2>
    <p class="why-media-lead">
      Au premier tour, chaque voix porte une conviction. Le second tour «&nbsp;contre&nbsp;» efface une partie de
      cette pluralité. Nous documentons les faits du 1er tour pour choisir en connaissance de cause.
    </p>
    <p class="why-media-tagline">
      Faits officiels · Sans classement éliminatoire · Vote <strong>pour</strong>, pas contre.
    </p>
    <nav class="pilier-shortcuts" aria-label="Raccourcis piliers">
      <a href="/analyses/assemblee-premier-tour">Simuler l’AN1T</a>
      <span aria-hidden="true">·</span>
      <a href="/analyses/presidentielle-2027-preparation">Dossier 2027</a>
      <span aria-hidden="true">·</span>
      <a href="/atlas/2022-presidentielle">Atlas 2022</a>
      <span aria-hidden="true">·</span>
      <a href="/sources">Sources</a>
    </nav>
  </section>

  <!-- Fil directeur + dual hémicycle (profondeur, après value prop) -->
  <section class="card fil-directeur" id="fil-directeur" aria-labelledby="fil-directeur-title">
    <p class="eyebrow">Fil directeur du site</p>
    <h2 id="fil-directeur-title">1er tour présidentiel → législatives — le différentiel visible</h2>
    <p>
      Sur <strong>chaque présidentielle</strong>&nbsp;: à gauche, l’Assemblée calée sur le
      <strong>1er tour présidentiel</strong> (Sainte-Laguë, proximité de programmes)&nbsp;; à droite,
      l’Assemblée réelle des <strong>législatives qui ont suivi</strong>. Tableau Δ sièges.
    </p>
    <p class="meta">
      <a href="/analyses/assemblee-premier-tour">Ouvrir le simulateur AN1T →</a>
      {' · '}
      <a href="/atlas">Tous les scrutins</a>
      {' · '}
      <a href="/atlas/2022-presidentielle">2022</a>
      {' · '}
      <a href="/atlas/2024-legislatives">Législatives 2024</a>
      {' · '}
      <a href="/analyses/gouvernance-an1t-droit">Droit &amp; gouvernance</a>
    </p>
    {
      dualHome && (
        <DualAssembleeCompare
          compact
          fullHref="/analyses/assemblee-premier-tour"
          firstRoundAlloc={dualHome.firstRound}
          realAlloc={dualHome.real}
          totalSeats={dualHome.totalSeats}
          t1Label={dualHome.t1Label}
          t2Label={dualHome.realLabel}
          firstRoundTitle={dualHome.firstRoundTitle}
          realTitle={dualHome.realTitle}
          firstRoundMeta={dualHome.firstRoundMeta}
          realMeta={dualHome.realMeta}
          realPending={dualHome.realPending}
          differential={dualHome.differential}
        />
      )
    }

  </section>

  <!-- Alerte citoyenne (conditions du débat public) -->
  <AlerteCitoyenne previewLimit={4} />

  {
    feed.enabled && feed.items.length > 0 && (
      <NewsFeed
        items={feed.items}
        limit={6}
        disclaimer={feed.disclaimer}
        fetchedAt={feed.fetched_at}
        feedsOk={feed.feeds_ok}
      />
    )
  }

  <section class="card highlight-card">
    <p class="eyebrow">Focus 2027 — Principal</p>
    <h3>
      <a href="/analyses/presidentielle-2027-preparation">Présidentielle 2027 : calendrier officiel & veille</a>
    </h3>
    <p>
      1er tour le <strong>18 avril 2027</strong>. Calendrier validé, candidatures en cours, programmes partiels intégrés (Attal, Bardella, Le Pen, Mélenchon, PS...).
    </p>
    <p class="meta">
      <a href="/analyses/presidentielle-2027-preparation">Lire le dossier complet 2027 →</a> · 
      <a href="/analyses/programmes/presidentielle-2027">Comparer les programmes →</a>
    </p>
  </section>

  <!-- Liste des candidatures déclarées (pas liste Conseil constitutionnel) -->
  <section class="card declared-board" id="candidatures-declarees-2027" aria-labelledby="declared-2027-title">
    <p class="eyebrow">Présidentielle 2027 — pluralité documentée</p>
    <h3 id="declared-2027-title">{candidaturesDeclarees.title}</h3>
    <p class="declared-disclaimer">{candidaturesDeclarees.disclaimer}</p>
    <p class="meta">
      <strong>{countDeclarees}</strong> candidatures déclarées / désignées en lice
      · {candidaturesDeclarees.entries.length} entrées au total (y compris conditionnelles &amp; retraits)
      · Mise à jour <time datetime={candidaturesDeclarees.updated}>{declaredUpdated}</time>
    </p>
    <p class="meta declared-legal">{candidaturesDeclarees.legal_note}</p>

    {
      declaredByCategory.map((group) => (
        <div class="declared-group">
          <h4 class="declared-group-title">
            {group.meta?.label ?? group.id}
            <span class="declared-count">{group.entries.length}</span>
          </h4>
          {group.meta?.description && <p class="meta declared-group-desc">{group.meta.description}</p>}
          <div class="declared-table-wrap" role="region" aria-label={group.meta?.label ?? group.id} tabindex="0">
            <table class="declared-table">
              <thead>
                <tr>
                  <th scope="col">Nom</th>
                  <th scope="col">Affiliation</th>
                  <th scope="col">X (officiel)</th>
                  <th scope="col">Statut</th>
                  <th scope="col">Depuis</th>
                  <th scope="col">Source</th>
                </tr>
              </thead>
              <tbody>
                {group.entries.map((c: CandidatureEntry) => {
                  const party = partyLogoForAffiliation(c.affiliation);
                  const xCand = xForCandidate(c.slug);
                  const xParty = xForAffiliation(c.affiliation);
                  return (
                    <tr class:list={[{ 'declared-row--retrait': c.category === 'retrait' }]}>
                      <td>
                        <strong>{c.name}</strong>
                        {c.note && <p class="poll-note">{c.note}</p>}
                      </td>
                      <td class="poll-aff">
                        <span class="party-logo-row">
                          <img
                            class="party-logo"
                            src={partyLogoSrc(party)}
                            alt=""
                            width="28"
                            height="28"
                            loading="lazy"
                            decoding="async"
                          />
                          <span class="party-logo-label">{c.affiliation}</span>
                        </span>
                      </td>
                      <td class="x-links">
                        {xCand?.url ? (
                          <a
                            class="x-link x-link--person"
                            href={xCand.url}
                            rel="noopener noreferrer"
                            title={`X — ${c.name}`}
                          >
                            {formatXHandle(xCand.handle)}
                          </a>
                        ) : (
                          <span class="meta">—</span>
                        )}
                        {xParty?.url && (
                          <>
                            {' '}
                            <a
                              class="x-link x-link--party"
                              href={xParty.url}
                              rel="noopener noreferrer"
                              title={`X — ${xParty.label ?? c.affiliation}`}
                            >
                              parti
                            </a>
                          </>
                        )}
                      </td>
                      <td>
                        <span class="poll-tag">{statusDeclareLabels[c.status] ?? c.status}</span>
                      </td>
                      <td class="declared-date">
                        <time datetime={c.declared_at}>{c.declared_at}</time>
                      </td>
                      <td class="poll-src">
                        <a href={c.source_url} rel="noopener noreferrer">
                          {c.source_label}
                        </a>
                      </td>
                    </tr>
                  );
                })}
              </tbody>
            </table>
          </div>
        </div>
      ))
    }

    <p class="meta" style="margin-top:1rem;">
      Sources :{' '}
      {candidaturesDeclarees.sources.map((s, i) => (
        <>
          {i > 0 && ' · '}
          <a href={s.url} rel="noopener noreferrer">
            {s.label}
          </a>
        </>
      ))}
      {' · '}
      <a href="/analyses/presidentielle-2027-preparation">Dossier 2027</a>
      {' · '}
      <a href="/analyses/programmes/presidentielle-2027">Programmes</a>
    </p>
  </section>

  <!-- Parcours utilisateur clair pour préparation 2027 -->
  <section class="card" aria-labelledby="parcours-2027">
    <h3 id="parcours-2027">Parcours recommandé pour se préparer au 1er tour</h3>
    <ol style="margin:0.5rem 0 0; padding-left:1.3rem; font-size:0.95rem; line-height:1.6;">
      <li><a href="/analyses/presidentielle-2027-preparation#calendrier">Consultez le calendrier officiel</a></li>
      <li><a href="/analyses/assemblee-premier-tour">Testez le simulateur interactif d’Assemblée (1er tour)</a> — ajustez % et comparez scénarios</li>
      <li><a href="/analyses/programmes-comparateur">Comparez les programmes des candidats</a></li>
      <li><a href="/atlas/2024-legislatives">Explorez l’Atlas et les 577 circonscriptions</a> (filtre + recherche)</li>
      <li><a href="/debats/assemblee-premier-tour">Participez au débat : une Assemblée sur le 1er tour ?</a></li>
    </ol>
    <p class="meta" style="margin-top:0.5rem;">Parcours accessible au clavier et mobile. Tout est pédagogique et sourcé.</p>
  </section>

  <section class="card">
    <p class="eyebrow">Outil clé 2027</p>
    <h3>
      <a href="/analyses/assemblee-premier-tour">Assemblée du Premier Tour 2027 — simulation interactive</a>
    </h3>
    <p>
      Hémicycle AN1T, projections de sièges à partir du 1er tour, scénarios et programmes candidats.
    </p>
    <p class="meta">
      <a href="/analyses/assemblee-premier-tour">Explorer la simulation 2027 →</a>
    </p>
  </section>

  <section class="content-section">
    <h3 class="section-title">Débats ouverts</h3>
    <ul class="card-grid">
      <li>
        <article class="card">
          <h3>
            <a href="/debats/assemblee-premier-tour">Faut-il une chambre élue sur le seul premier tour ?</a>
            <span class="status-badge status-badge--open">nouveau</span>
          </h3>
          <p>
            Simulation 2024 : hémicycle virtuel AN1T vs Assemblée réelle — carte et comparatif chiffré.
          </p>
        </article>
      </li>
      <li>
        <article class="card">
          <h3>
            <a href="/debats/vote-utile-pluralite">Le vote utile respecte-t-il la pluralité démocratique ?</a>
          </h3>
          <p>
            Positions sourcées des deux côtés — discussion modérée sur GitHub. Pas de classement
            éliminatoire.
          </p>
          <p class="meta">
            <a href="/debats">Voir tous les débats →</a>
          </p>
        </article>
      </li>
    </ul>
  </section>

  <section class="content-section">
    <h3 class="section-title">Atlas & analyses</h3>
    <ul class="card-grid">
      <li>
        <article class="card">
          <h3>
            <a href="/analyses/assemblee-premier-tour">Assemblée du premier tour — simulation 2024</a>
            <span class="status-badge status-badge--open">nouveau</span>
          </h3>
          <p>Hémicycle virtuel AN1T (577 sièges) vs Assemblée élue — comparatif chiffré.</p>
        </article>
      </li>
      <li>
        <article class="card">
          <h3><a href="/atlas/2024-legislatives">Législatives 2024 — 1er tour</a></h3>
          <p>RN 29,26 % · UG 27,99 % · ENS 20,04 % — 577 circonscriptions, data.gouv.fr.</p>
        </article>
      </li>
      <li>
        <article class="card">
          <h3><a href="/analyses/presidentielle-distorsion">Distorsion 1er / 2nd tour</a></h3>
          <p>Pluralité au premier tour, duel binaire au second — 2017 et 2022.</p>
        </article>
      </li>
      <li>
        <article class="card">
          <h3><a href="/analyses/legislatives-2024-desistements">Législatives 2024 — désistements</a></h3>
          <p>306 triangulaires potentielles → 89 après 215 désistements officiels.</p>
        </article>
      </li>
      <li>
        <article class="card">
          <h3><a href="/atlas/2022-presidentielle">Présidentielle 2022 — 1er tour</a></h3>
          <p>Macron 27,85 % · Le Pen 23,15 % · Mélenchon 21,95 %.</p>
        </article>
      </li>
      <li>
        <article class="card">
          <h3><a href="/atlas/2017-presidentielle">Présidentielle 2017 — 1er tour</a></h3>
          <p>Macron 24,01 % · Le Pen 21,30 % · quadruple polarisation.</p>
        </article>
      </li>
    </ul>
  </section>

  <!-- 
    PRINCIPE DE BASE (intégralité du site, sauf live YouTube) :
    - On expose des idées, pas des idéologies.
    - Aucun signe religieux ostentatoire, et encore moins toute autorité religieuse.
    - ne doit être diffusée.
    - Blocage immédiat de tout contenu contrevenant.
  -->
  <section class="card principles">
    <h3>Notre ligne</h3>
    <ul>
      <li><strong>Zéro biais · zéro parti pris</strong> — le média ne soutient aucun camp.</li>
      <li><strong>Transparence des couleurs politiques</strong> — posts et intervenants affichés avec teinte d’idées 1er tour (pas une carte d’adhésion).</li>
      <li><strong>Modératrice IA</strong> — aucune autorité religieuse ou idéologique ne passe les fourches caudines ; les idées politiques du débat restent bienvenues.</li>
      <li>Sources officielles traçables (<a href="/sources">data.gouv.fr</a>)</li>
      <li>Comparaisons factuelles, sans classement éliminatoire</li>
      <li><strong>Democracy Over Elimination</strong> — pluralité du 1er tour, sans ranking ni favori.</li>
      <li>Lisibilité maximale (WCAG 2.2 AA+), revue humaine après filtre IA.</li>
      <li>Document complet : <a href="/charte"><strong>Charte publique</strong></a>.</li>
    </ul>
  </section>

  <!-- Visuels 2027 — croquis humoristiques style X (laïcs, civiques) -->
  <section class="visuals-section" id="visuels-2027" aria-labelledby="visuels-2027-title">
    <h3 class="section-title" id="visuels-2027-title">Croquis humoristiques style X &amp; animations — Élection 2027</h3>
    <p class="lead" style="max-width: 52rem;">
      Pour incarner le premier tour : pluralité réelle, démocratie sans élimination prématurée.
      <strong>Croquis encre + aquarelle</strong> façon fil X — légers sur les <em>mécanismes</em>, jamais sur les personnes.
      Zéro signe religieux, zéro logo de parti, zéro caricature de camp.
    </p>

    <div class="visuals-grid">
      <figure class="visual-card" style="--i: 0">
        <img src="/illustrations/2027/democracy-over-elimination.jpg" alt="Croquis humoristique : citoyen brisant les flèches du second tour — Democracy Over Elimination" loading="lazy" decoding="async" />
        <figcaption>
          <span class="visual-label">Croquis X</span>
          <strong>Democracy Over Elimination</strong>
          <span class="visual-wit">Le second tour aime les flèches. Le premier préfère les bulletins.</span>
        </figcaption>
      </figure>
      <figure class="visual-card" style="--i: 1">
        <img src="/illustrations/2027/pluralite-1er-tour.jpg" alt="Croquis humoristique : urne débordant de bulletins colorés — pluralité du 1er tour" loading="lazy" decoding="async" />
        <figcaption>
          <span class="visual-label">Croquis X</span>
          <strong>La pluralité au 1er tour</strong>
          <span class="visual-wit">Trop de couleurs dans l’urne ? C’est le pays qui parle — pas un bug.</span>
        </figcaption>
      </figure>
      <figure class="visual-card" style="--i: 2">
        <img src="/illustrations/2027/dessin-urne-2027.jpg" alt="Croquis humoristique : urne transparente et miroir du 1er tour — miroir de la France" loading="lazy" decoding="async" />
        <figcaption>
          <span class="visual-label">Croquis X</span>
          <strong>L’urne-miroir du 1er tour</strong>
          <span class="visual-wit">Transparente comme les chiffres… quand on se donne la peine de les regarder.</span>
        </figcaption>
      </figure>
      <figure class="visual-card" style="--i: 3">
        <img src="/illustrations/2027/photo-citoyens-data-2027.jpg" alt="Croquis humoristique : citoyens consultent les données ouvertes du 1er tour" loading="lazy" decoding="async" />
        <figcaption>
          <span class="visual-label">Croquis X</span>
          <strong>Données &amp; citoyens</strong>
          <span class="visual-wit">La fierté civique, version open data : moins de slogans, plus de sources.</span>
        </figcaption>
      </figure>
      <figure class="visual-card" style="--i: 4">
        <img src="/illustrations/2027/meme-1er-tour-ne-ment-pas.jpg" alt="Croquis mème : Le 1er tour ne ment pas — bulletin-miroir" loading="lazy" decoding="async" />
        <figcaption>
          <span class="visual-label">Croquis mème</span>
          <strong>Le 1er tour ne ment pas</strong>
          <span class="visual-wit">Le second, lui, fait des arrangements. On documente l’écart — sans le moraliser.</span>
        </figcaption>
      </figure>
      <figure class="visual-card" style="--i: 5">
        <img src="/illustrations/2027/meme-vote-pensee.jpg" alt="Croquis mème : Je vote ce que je pense au premier tour" loading="lazy" decoding="async" />
        <figcaption>
          <span class="visual-label">Croquis mème</span>
          <strong>Votez ce que vous pensez</strong>
          <span class="visual-wit">Le « contre » peut attendre le second tour. Au premier, on dit « pour ».</span>
        </figcaption>
      </figure>
      <figure class="visual-card" style="--i: 6">
        <img src="/illustrations/2027/illustration-voix-egales.jpg" alt="Croquis humoristique : balance des voix égales au premier tour" loading="lazy" decoding="async" />
        <figcaption>
          <span class="visual-label">Croquis X</span>
          <strong>Toutes les voix comptent</strong>
          <span class="visual-wit">Même celles qu’on redistribue ensuite. D’où l’intérêt de les lire avant.</span>
        </figcaption>
      </figure>
    </div>

    <div class="video-embed">
      <h4>Animation — croquis 1er tour (6&nbsp;s)</h4>
      <video
        class="gallery-video"
        controls
        autoplay
        muted
        loop
        playsinline
        preload="metadata"
        width="100%"
        poster="/illustrations/2027/hero-premier-tour-2027.webp"
      >
        <source src="/videos/2027/hero-voting-animation.mp4" type="video/mp4" />
        Votre navigateur ne supporte pas la vidéo.
      </video>
      <p class="meta">
        Six secondes : croquis animé, urne transparente, idées qui bougent — on vote <em>pour</em>, pas pour éliminer.
      </p>
    </div>
  </section>
</BaseLayout>

<style>
  /* =====================================================
     POURQUOI CE MÉDIA — en tête de page
  ===================================================== */
  .why-media {
    margin: 0 0 1.75rem;
    padding: 1.25rem 1.35rem 1.35rem;
    background: var(--card);
    border: 1px solid var(--border);
    border-radius: var(--radius-lg, 12px);
    border-left: 4px solid var(--accent, #2563a8);
    max-width: 52rem;
  }
  .why-media h2 {
    margin: 0 0 0.65rem;
    font-size: 1.45rem;
    line-height: 1.25;
    color: #0a0a0a !important;
  }
  .why-media-lead {
    margin: 0 0 0.75rem;
    font-size: 1.05rem;
    line-height: 1.55;
    color: #0a0a0a !important;
    max-width: 46rem;
  }
  .why-media-tagline {
    margin: 0;
    font-size: 0.98rem;
    line-height: 1.45;
    color: #0a0a0a !important;
    font-weight: 600;
  }
  .why-media-tagline strong {
    color: #0a0a0a !important;
  }
  @media (prefers-color-scheme: dark) {
    .why-media h2,
    .why-media-lead,
    .why-media-tagline,
    .why-media-tagline strong {
      color: #f5f2eb !important;
    }
  }

  /* =====================================================
     HERO REDESIGN — Professional, high-contrast, inclusive
     - WCAG 2.2 AA focus (contrast + visible focus states)
     - Mobile-first
     - Neutral civic aesthetic
     - Strong dark mode support (per design brief)
  ===================================================== */

  .hero {
    margin: -0.25rem -1rem 2.5rem;
    padding: 1.75rem 1rem 2rem;
    background: transparent;
    border-bottom: 1px solid var(--border);
  }

  .hero-badge {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.95rem; /* improved for readability across ages and vision levels */
    font-family: system-ui, sans-serif;
    background: var(--card);
    border: 1px solid var(--border);
    padding: 0.35rem 0.85rem;
    border-radius: 999px;
    margin-bottom: 1rem;
    color: var(--muted);
  }

  .badge-dot {
    display: inline-block;
    width: 6px;
    height: 6px;
    background: var(--accent);
    border-radius: 50%;
    animation: lmdpt-pulse 1.8s ease-in-out infinite;
  }

  .hero-badge .sep { opacity: 0.4; }

  .hero-grid {
    display: grid;
    gap: 1.5rem;
    grid-template-columns: 1fr;
  }

  @media (min-width: 860px) {
    .hero-grid {
      grid-template-columns: 1fr 1fr;
      align-items: start;
      gap: 2rem;
    }
  }

  .hero-inner {
    max-width: 42rem;
  }

  .hero-title {
    font-size: clamp(1.65rem, 4.2vw, 2.35rem);
    line-height: 1.1;
    margin: 0 0 0.85rem;
    letter-spacing: -0.025em;
    font-weight: 700;
    color: var(--fg) !important;
  }

  .hero-lead {
    font-size: 1.1rem;
    line-height: 1.5;
    margin: 0 0 1.1rem;
    color: var(--fg) !important;
    max-width: 38ch;
  }

  .cta-row {
    display: flex;
    flex-wrap: wrap;
    gap: 0.6rem;
    margin: 0 0 0.75rem;
  }

  .btn-primary {
    background: var(--accent) !important;
    color: var(--on-accent) !important;
    border: 2px solid var(--accent) !important;
  }

  .btn-primary:hover {
    filter: brightness(1.08);
    color: var(--on-accent) !important;
  }
  .btn-secondary {
    background: var(--card) !important;
    color: var(--accent) !important;
    border: 2px solid var(--accent) !important;
  }
  .highlight-card h3 {
    margin: 0.35rem 0 0.65rem;
    font-size: 1.15rem;
    color: var(--fg) !important;
  }
  .highlight-card h3 a {
    color: var(--accent) !important;
    text-decoration: none;
  }
  .highlight-card h3 a:hover {
    filter: brightness(1.1);
  }
  .highlight-card p {
    color: var(--fg) !important;
  }
  .content-section {
    margin-bottom: 2rem;
  }
  .principles ul {
    margin: 0.5rem 0 0;
    padding-left: 1.2rem;
  }

  /* 3 piliers éditoriaux (P15-1) — grid 1→3, contrastes WCAG, focus-ring */
  .hero-cards {
    display: grid;
    grid-template-columns: 1fr;
    gap: 0.75rem;
    margin-top: 1.5rem;
  }

  @media (min-width: 640px) {
    .hero-cards {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  .hero-card {
    display: flex;
    gap: 0.75rem;
    padding: 1rem 1rem;
    background: var(--card) !important;
    border: 1px solid var(--border);
    border-radius: var(--radius-lg);
    text-decoration: none;
    color: var(--fg) !important;
    transition: border-color 0.2s ease, box-shadow 0.2s ease, transform 0.2s cubic-bezier(0.34, 1.2, 0.64, 1);
    animation: lmdpt-rise 0.55s ease both;
    animation-delay: calc(0.12s * var(--card-i, 0));
    min-height: 6.5rem;
  }

  .hero-card:nth-child(1) { --card-i: 1; }
  .hero-card:nth-child(2) { --card-i: 2; }
  .hero-card:nth-child(3) { --card-i: 3; }

  .pilier-card[data-pilier='faits'] {
    border-top: 3px solid #1d4ed8;
  }
  .pilier-card[data-pilier='pluralite'] {
    border-top: 3px solid #059669;
  }
  .pilier-card[data-pilier='doe'] {
    border-top: 3px solid var(--accent, #c2410c);
  }

  .pilier-kicker {
    font-size: 0.7rem;
    font-weight: 700;
    letter-spacing: 0.04em;
    text-transform: uppercase;
    color: var(--muted) !important;
    margin-bottom: 0.15rem;
  }

  .hero-card:hover {
    border-color: var(--accent);
    box-shadow: 0 8px 20px -4px rgb(0 0 0 / 0.12);
    transform: translateY(-4px);
  }

  .hero-card:focus-visible {
    outline: 3px solid var(--accent);
    outline-offset: 2px;
  }

  .hero-card .card-icon {
    font-size: 1.35rem;
    line-height: 1;
    margin-top: 2px;
    flex-shrink: 0;
  }

  .hero-card .card-title {
    font-weight: 700;
    font-size: 0.97rem;
    line-height: 1.25;
    margin-bottom: 0.25rem;
    color: var(--fg) !important;
  }

  .hero-card .card-desc {
    font-size: 0.88rem;
    color: var(--muted) !important;
    line-height: 1.45;
  }

  .hero-disclaimer {
    margin: 0.85rem 0 0;
    max-width: 70ch;
    line-height: 1.45;
  }

  .pilier-shortcuts {
    margin-top: 0.85rem;
    display: flex;
    flex-wrap: wrap;
    gap: 0.35rem 0.5rem;
    font-size: 0.92rem;
  }

  .pilier-shortcuts a {
    font-weight: 600;
  }

  .pilier-shortcuts a:focus-visible {
    outline: 3px solid var(--accent);
    outline-offset: 2px;
  }

  .hero-badge {
    background: var(--card) !important;
    color: var(--muted) !important;
    border-color: var(--border) !important;
  }

  /* Hero visual — croquis animé */
  .hero-visual {
    width: 100%;
    position: relative;
  }

  .hero-visual--animated {
    isolation: isolate;
  }

  .hero-img {
    width: 100%;
    height: auto;
    max-height: 460px;
    object-fit: cover;
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow);
    border: 1px solid var(--border);
    display: block;
    background: var(--card);
  }

  .hero-video {
    position: relative;
    z-index: 1;
    animation: lmdpt-float 5.5s ease-in-out infinite;
  }

  /* Fallback image hidden when video can play; shown if video fails / reduced-motion */
  .hero-static-fallback {
    display: none;
  }

  @media (min-width: 860px) {
    .hero-img {
      max-height: 380px;
    }
  }

  .hero-title {
    animation: lmdpt-rise 0.55s ease both;
  }
  .hero-lead {
    animation: lmdpt-rise 0.55s ease 0.08s both;
  }
  .cta-row {
    animation: lmdpt-rise 0.55s ease 0.14s both;
  }
  .hero-badge {
    animation: lmdpt-rise 0.45s ease both;
  }

  .visuals-section {
    margin-top: 2.5rem;
  }
  .visuals-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
    gap: 1rem;
    margin: 1.25rem 0 2rem;
  }
  .visual-card {
    margin: 0;
    background: var(--card);
    border: 1px solid var(--border);
    border-radius: var(--radius-lg);
    overflow: hidden;
    box-shadow: var(--shadow);
    transition: transform 0.25s cubic-bezier(0.34, 1.2, 0.64, 1), box-shadow 0.25s ease, border-color 0.2s ease;
    animation: lmdpt-rise 0.55s ease both;
    animation-delay: calc(0.07s * var(--i, 0));
  }
  .visual-card:hover {
    transform: translateY(-5px) rotate(-0.4deg);
    box-shadow: 0 12px 28px -8px rgb(0 0 0 / 0.16);
    border-color: var(--accent);
  }
  .visual-card img {
    width: 100%;
    height: auto;
    display: block;
    aspect-ratio: 1 / 1;
    object-fit: cover;
    transition: transform 0.45s ease;
  }
  .visual-card:hover img {
    transform: scale(1.04);
  }
  .gallery-video {
    max-width: 820px;
    width: 100%;
    border-radius: 8px;
    box-shadow: var(--shadow);
    border: 1px solid var(--border);
    background: var(--card);
  }

  @keyframes lmdpt-pulse {
    0%, 100% { opacity: 1; transform: scale(1); box-shadow: 0 0 0 0 color-mix(in srgb, var(--accent) 45%, transparent); }
    50% { opacity: 0.85; transform: scale(1.15); box-shadow: 0 0 0 6px transparent; }
  }
  @keyframes lmdpt-float {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-6px); }
  }
  @keyframes lmdpt-rise {
    from { opacity: 0; transform: translateY(12px); }
    to { opacity: 1; transform: translateY(0); }
  }

  @media (prefers-reduced-motion: reduce) {
    .badge-dot,
    .hero-video,
    .hero-title,
    .hero-lead,
    .cta-row,
    .hero-badge,
    .hero-card,
    .visual-card {
      animation: none !important;
    }
    .hero-card:hover,
    .visual-card:hover,
    .visual-card:hover img {
      transform: none;
    }
    .hero-video {
      display: none;
    }
    .hero-static-fallback {
      display: block;
    }
  }
  .visual-card figcaption {
    display: flex;
    flex-direction: column;
    gap: 0.2rem;
    padding: 0.65rem 0.8rem 0.75rem;
    font-size: 0.9rem;
    color: var(--fg);
    background: var(--bg);
    border-top: 1px solid var(--border);
    line-height: 1.35;
  }
  .visual-card .visual-label {
    font-size: 0.72rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.04em;
    color: var(--muted);
  }
  .visual-card figcaption strong {
    font-size: 0.95rem;
    color: var(--fg);
  }
  .visual-card .visual-wit {
    font-size: 0.86rem;
    color: var(--muted);
    font-style: italic;
    line-height: 1.4;
  }
  .video-embed {
    margin-top: 1.5rem;
    max-width: 860px;
  }
  .video-embed h4 {
    margin-bottom: 0.5rem;
    font-size: 1.05rem;
  }

  /* === Candidatures déclarées 2027 (accueil) === */
  .declared-board {
    margin-top: 0.25rem;
  }
  .declared-disclaimer,
  .declared-legal {
    font-size: 0.95rem;
    line-height: 1.5;
    color: var(--fg) !important;
    margin: 0.5rem 0 0.65rem;
    max-width: 62rem;
  }
  .declared-group {
    margin-top: 1.25rem;
  }
  .declared-group-title {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 1.05rem;
    margin: 0 0 0.35rem;
    color: var(--fg) !important;
  }
  .declared-count {
    font-size: 0.8rem;
    font-weight: 600;
    padding: 0.1rem 0.45rem;
    border-radius: 999px;
    border: 1px solid var(--border);
    background: var(--bg);
    color: var(--muted) !important;
  }
  .declared-group-desc {
    margin: 0 0 0.5rem;
  }
  .declared-table-wrap {
    overflow-x: auto;
    margin: 0.5rem 0 0;
    border: 1px solid var(--border);
    border-radius: var(--radius-lg);
    -webkit-overflow-scrolling: touch;
  }
  .declared-table {
    width: 100%;
    border-collapse: collapse;
    font-size: 0.92rem;
    min-width: 640px;
  }
  .declared-table th,
  .declared-table td {
    padding: 0.65rem 0.75rem;
    text-align: left;
    vertical-align: top;
    border-bottom: 1px solid var(--border);
    color: var(--fg) !important;
  }
  .declared-table th {
    background: var(--bg);
    font-weight: 600;
    font-size: 0.85rem;
  }
  .declared-table tbody tr:last-child td {
    border-bottom: none;
  }
  .declared-row--retrait {
    opacity: 0.72;
  }
  .declared-date {
    font-variant-numeric: tabular-nums;
    white-space: nowrap;
    color: var(--muted) !important;
  }
  .poll-tag {
    display: inline-block;
    font-size: 0.72rem;
    font-weight: 600;
    letter-spacing: 0.02em;
    padding: 0.12rem 0.4rem;
    border-radius: 999px;
    border: 1px solid var(--border);
    background: var(--bg);
    color: var(--muted) !important;
    white-space: nowrap;
    margin-left: 0.35rem;
    vertical-align: middle;
  }
  .poll-note {
    margin: 0.25rem 0 0;
    font-size: 0.82rem;
    color: var(--muted) !important;
    line-height: 1.35;
  }
  .x-links {
    white-space: nowrap;
    font-size: 0.88rem;
  }
  .x-link {
    color: var(--accent, #2563a8) !important;
    text-decoration: none;
    font-weight: 600;
  }
  .x-link:hover,
  .x-link:focus-visible {
    text-decoration: underline;
  }
  .x-link--party {
    font-weight: 500;
    font-size: 0.8rem;
    opacity: 0.9;
  }
  .poll-aff {
    font-size: 0.88rem;
    color: var(--muted) !important;
    max-width: 16rem;
  }
  .party-logo-row {
    display: inline-flex;
    align-items: center;
    gap: 0.55rem;
    max-width: 100%;
  }
  .party-logo {
    flex: 0 0 auto;
    width: 28px;
    height: 28px;
    border-radius: 7px;
    object-fit: cover;
    border: 1px solid color-mix(in srgb, var(--border) 80%, transparent);
    background: var(--bg);
    box-shadow: 0 1px 2px rgba(0, 0, 0, 0.06);
  }
  .party-logo-label {
    line-height: 1.25;
    color: var(--fg) !important;
  }
  .poll-src {
    font-size: 0.82rem;
    line-height: 1.35;
    max-width: 16rem;
  }
  .poll-src a {
    color: var(--accent) !important;
  }
</style>