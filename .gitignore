# dependencies
node_modules/

# build
dist/
.astro/

# cache (données data.gouv.fr — régénérable via npm run sync:data)
data/cache/*.json

# env / secrets — ne jamais commiter (voir mediconvoi/docs/WORKSPACE-ENV-DB-HOMOGENEITE.md)
# ⚠ Ce projet n'utilise PAS la DB mediconvoi/iarbre — .env minimal uniquement (Astro + DNS).
.env
.env.*
!.env.example

# OS / IDE
.DS_Store
Thumbs.db
*.log
