# dependencies
node_modules/

# build
dist/
.astro/

# cache (données data.gouv.fr — régénérable via npm run sync:data)
data/cache/*.json

# env / secrets (copie locale possible depuis mediconvoi/backend/.env — ne jamais commiter)
.env
.env.*
!.env.example

# OS / IDE
.DS_Store
Thumbs.db
*.log
