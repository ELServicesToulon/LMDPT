# dependencies
node_modules/

# build
dist/
.astro/

# cache (données data.gouv.fr — régénérable via npm run sync:data)
data/cache/*.json

# env / secrets — ne jamais commiter (voir mediconvoi/docs/WORKSPACE-ENV-DB-HOMOGENEITE.md)
# Base minimale : ../mediconvoi/backend/.env — ce .env = overlay LMDPT (ne pas supprimer).
.env
.env.*
!.env.example

# OS / IDE
.DS_Store
Thumbs.db
*.log
comments-api/data/
comments-api/node_modules/

# millésimes hero daily (le live.* versionné reste suivi)
public/illustrations/2027/hero-daily-20[0-9][0-9]-*.jpg
public/videos/2027/daily/
